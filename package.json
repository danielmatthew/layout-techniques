{
  "name": "flex-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx browser-sync start --server --files '**/*.css, **/*.html, **/*.js, !node_modules/**/*' --directory"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serve": "^10.1.1"
  }
}
